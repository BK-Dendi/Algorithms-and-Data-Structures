void solve() {
    int n, m, k;
    cin >> n >> m >> k;
    vector<string> res(n);
    
    for(string &s: res) {
        cin >> s;
    }
    
    for(int i = 0; i < n * k; i++) {
        for(int j = 0; j < m * k; j++) {
            cout << res[i/k][j/k];
        }
        cout << "\n";
    }
}
